<p id="notice"><%= notice %></p>
<%- debugger %>
<p>
  <b>Title:</b>
  <%= @project.project_title %>
</p>

<%= select_tag 'parent_id', options_for_select(nested_set_options(Project) {|i, level| "|#{'-' * level} #{i.project_title}" } ) %>
<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
